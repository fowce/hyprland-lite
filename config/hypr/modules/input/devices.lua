-- Per-device examples belong here.
-- Keep them disabled until a real device name has been confirmed with `hyprctl devices`.

-- hl.device({
--     name = "example-mouse",
--     sensitivity = -0.5,
-- })
