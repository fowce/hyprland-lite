# Palettes

Palette files define colors only. They do not define layout, spacing, modules, keybinds, or behavior.

Use `~/.config/theme/apply-palette.sh <name>` after installation to switch colors while keeping the same theme.
