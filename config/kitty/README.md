# Kitty

This directory contains the repository-managed Kitty terminal profile.

Files:

- `kitty.conf`: main terminal settings;
- `theme.conf`: neutral dark color palette;
- `custom.conf`: optional local override file, not required by the repo.
