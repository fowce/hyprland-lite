# Theme Colors

This directory contains generated color files.

The layout and component styling live in their own config files. Palette switching changes only colors.

After installation:

```bash
~/.config/theme/apply-palette.sh warm
```

Repository development mode:

```bash
./config/theme/apply-palette.sh warm --repo-root "$PWD" --no-reload
```
