# Config Source

This directory is the source tree copied by `install.sh`.

Do not copy these files into `~/.config` manually while the repository is still under construction.
The installer refuses to run until `.install-ready` contains `ready`.
