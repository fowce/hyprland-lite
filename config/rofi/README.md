# Rofi

Rofi themes and small menu configs live here.

Menus:

- application launcher;
- clipboard history;
- workspace switcher;
- power fallback;
- screenshot/OCR prompts.

The style is intentionally plain: search input on top, list below, generated
colors imported from `~/.config/theme/colors.rasi`.
